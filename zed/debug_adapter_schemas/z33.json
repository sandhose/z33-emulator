{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Z33 Debug Adapter Configuration",
  "type": "object",
  "properties": {
    "adapter": {
      "type": "string",
      "enum": ["z33"]
    },
    "label": {
      "type": "string",
      "description": "Human-readable name for this debug configuration",
      "examples": ["Debug main.s"]
    },
    "program": {
      "type": "string",
      "description": "Path to the Z33 assembly file to debug",
      "examples": ["main.s", "samples/fact.s"]
    },
    "entrypoint": {
      "type": "string",
      "description": "Label to start execution from. Defaults to whichever of main, start, run, or entry is defined in the program.",
      "examples": ["main", "start"]
    },
    "stopOnEntry": {
      "type": "boolean",
      "description": "Pause the debugger before executing the first instruction.",
      "default": false
    }
  },
  "required": ["program", "adapter"],
  "additionalProperties": false
}
